This is a CLI blackjack game project built with TypeScript. Core engine functionality is in root/packages/blackjack and the presentation lives in apps/cli.

DO NOT run pty tests if it is going to take longer than 1 minute. Typecheck and tsc --no-emit are fine on their own. Notify if this causes an actual issue.